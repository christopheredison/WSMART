<div class="rounded-3 border bg-light p-4">
  <div class="d-flex flex-wrap align-items-center gap-2 mb-2">
    <span class="badge bg-primary">Layer {{ $layerNumber }}</span>
    <span class="text-muted">{{ $dashboardName }}</span>
  </div>
  <h5 class="mb-1">{{ $layerName }}</h5>
  <p class="text-muted mb-0">
    Anda sedang berada di {{ $dashboardName }} — {{ $layerName }}.
  </p>
</div>
